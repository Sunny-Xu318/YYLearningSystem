# 天机学堂 - 在线职业教育平台

## 项目描述

天机学堂是一个基于Spring Cloud微服务架构的在线职业技能培训平台，采用前后端分离设计，前端使用Vue3+Element Plus/TDesign，后端基于Spring Cloud Alibaba构建16个微服务模块，集成了课程管理、在线学习、支付交易、营销推广等核心业务功能，支持高并发访问和海量数据存储，为学员提供完整的在线学习体验。

## 项目内容

### 1. 分布式锁组件设计
**技术方案**：基于Redisson + AOP + 自定义注解实现零侵入分布式锁组件  
**解决问题**：解决高并发场景下订单创建、库存扣减等业务的数据一致性问题，通过注解方式实现加锁逻辑，支持可重入锁、公平锁、读写锁等多种锁类型，提升了代码可维护性和系统并发性能。

### 2. 视频播放进度记录系统
**技术方案**：采用Redis缓存 + 定时任务批量持久化的异步处理方案  
**解决问题**：解决视频播放进度实时记录的高并发写入问题，通过Redis缓存接收进度更新请求，定时任务批量写入MySQL，将数据库压力降低90%以上，支持秒级精度的进度记录和数万TPS的并发写入。

### 3. 优惠券高并发领券方案
**技术方案**：基于Redis Lua脚本实现原子性库存扣减，结合MapReduce思想实现智能优惠券推荐  
**解决问题**：解决秒杀场景下优惠券超发问题，通过Lua脚本保证库存扣减的原子性，避免超发风险；智能推荐算法根据购物车商品自动计算最优优惠券组合，提升用户体验和平台收益。

### 4. 多级缓存架构优化
**技术方案**：构建客户端 → Nginx缓存 → Caffeine本地缓存 → Redis → MySQL五级缓存架构  
**解决问题**：解决热点数据访问导致的数据库压力问题，通过多级缓存将热点数据访问速度提升1000倍，数据库压力降低95%，接口响应时间控制在50ms以内，显著提升了系统整体性能。

